# screensaver/

Idle "deep field" vistas: `screensaver01.mp4` ... `screensaverNN.mp4`.
After ~75s idle the bridge crossfades through these at half speed with
scene names from the SAVER_NAMES list in `panel_web.py`. Without local
files it falls back to Wikimedia space imagery. Gitignored.
